<?php
include '../main/koneksi.php';

$pjname = "";
$querywhere = "";
if (isset($_GET['query']))
{	
	$pjname = $_GET['query'];
	$pjname = strtoupper($pjname);
	$querywhere = " AND UPPER(FULL_NAME) LIKE '%$pjname%' ";
}

$query = "SELECT DISTINCT FULL_NAME AS DATA_VALUE, FULL_NAME AS DATA_NAME FROM APPS.PER_PEOPLE_F WHERE EFFECTIVE_END_DATE > SYSDATE $querywhere ORDER BY FULL_NAME ASC";
$result = oci_parse($con, $query);
	oci_execute($result);
	while($row = oci_fetch_row($result))
	{
		$record = array();
		$record['DATA_VALUE']=$row[0];
		$record['DATA_NAME']=$row[1];
		$data[]=$record;
	}

	echo json_encode($data);
?>